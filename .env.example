# Argentum Engine - Local Development Environment
# Copy this file to .env: just init
# These variables are loaded by `just server`

# =============================================================================
# Redis Configuration
# =============================================================================
# Enable Redis for session persistence (games survive server restarts)
CACHE_REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_TTL_MINUTES=1440

# =============================================================================
# Server Configuration
# =============================================================================
# Enable development endpoints (Swagger UI, debug routes)
# WARNING: Never enable in production!
GAME_DEV_ENDPOINTS_ENABLED=true

# =============================================================================
# Admin Configuration
# =============================================================================
# Password for the admin game replay feature (GET /api/admin/games)
# Access the replay UI at http://localhost:5173/admin
GAME_ADMIN_PASSWORD=admin

# =============================================================================
# Easter Eggs
# =============================================================================
# Name-based joke cards slipped into a deck (EasterEggDeckInjector). Off in production —
# the deploy never sets this — so keep it local-only.
GAME_EASTER_EGGS_ENABLED=true

# =============================================================================
# Set Configuration
# =============================================================================
# Enable incomplete/development sets locally
GAME_SETS_DOMINARIA_ENABLED=true

# =============================================================================
# AI Opponent Configuration
# =============================================================================
# Enable the AI opponent feature
# Local dev defaults to the built-in engine. Set GAME_AI_MODE=llm to use chat/Ollama.
GAME_AI_ENABLED=false

# LLM provider base URL (any OpenAI-compatible API: OpenRouter, Ollama, LiteLLM, vLLM, etc.)
# Default: http://localhost:11434/v1 (Ollama)
# GAME_AI_BASE_URL=https://openrouter.ai/api/v1        # OpenRouter
# GAME_AI_BASE_URL=http://host.docker.internal:11434/v1 # Ollama from Docker

# Shared OpenRouter API key for chat models and Jev
# OPENROUTER_API_KEY=sk-or-xxxx
# Optional API key override for another chat provider (Ollama ignores it)
# GAME_AI_API_KEY=your-provider-key

# Model to use for AI gameplay decisions (default: qwen3.6-plus for Ollama)
# GAME_AI_MODEL=nvidia/nemotron-3-super-120b-a12b              # OpenRouter format
# Model to use for AI deckbuilding (defaults to GAME_AI_MODEL if not set)
# GAME_AI_DECKBUILDING_MODEL=nvidia/nemotron-3-super-120b-a12b

# Jev opponent (local profile): enable AI above and set GAME_AI_MODE=jev.
# Uses Decisions API, independently of the Ollama/chat settings above.
# GAME_AI_MODE=jev
# GAME_AI_JEV_MODEL=typesafe/jev-1.13
# GAME_AI_JEV_TIMEOUT_MS=30000

# =============================================================================
# Accounts / persistence (PostgreSQL + magic-link auth) — OPT-IN
# =============================================================================
# Off by default: the server runs anonymous + in-memory with no database.
# To enable saved decks + stats, set ACCOUNTS_ENABLED=true, CLEAR the auto-config
# exclude (must be empty), and point ACCOUNTS_DB_* at Postgres.
# See docs/accounts-and-persistence.md.
#
# ACCOUNTS_ENABLED=true
# ACCOUNTS_AUTOCONFIG_EXCLUDE=
# ACCOUNTS_DB_URL=jdbc:postgresql://localhost:5432/argentum
# ACCOUNTS_DB_USER=argentum
# ACCOUNTS_DB_PASSWORD=argentum
#
# HMAC secret for signing auth tokens (blank => random per restart). Set in prod.
# ACCOUNTS_AUTH_SECRET=change-me-to-a-long-random-string
# Public base URL the magic link points at (the web client's origin).
# APP_BASE_URL=http://localhost:5173
# From address on the sign-in email.
# ACCOUNTS_FROM_EMAIL=no-reply@wingedsheep.com
#
# --- Mailgun SMTP (leave MAIL_USERNAME blank in dev to log the link to the console) ---
# MAIL_HOST=smtp.mailgun.org
# MAIL_PORT=2525            # default; many hosts block 25/465/587, Mailgun also listens on 2525
# MAIL_USERNAME=postmaster@your-domain.mailgun.org
# MAIL_PASSWORD=your-mailgun-smtp-password
